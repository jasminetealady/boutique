
<div class="main">
  <% @items.each do |item| %>
    <div class="shop-item">
      <%= image_tag item.picture %>
      <p><%= link_to item.name, item_path(item) %></p>
      <p><%= number_to_currency(item.price) %></p>

    </div>
  <%end%>

</div>
